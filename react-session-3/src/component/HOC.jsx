import React from 'react';

export default function HOC(props) {

    const [state, useState] = useState(true)
  return (
    <div>
        {this.props.content}
        {true ? }
        </div>
    )
}
